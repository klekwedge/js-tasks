// Повторить строку n раз
// Напишите функцию repeatStr(str, n),
// которая вовращает строку повторяемую определённое
// количество раз.

function repeatStr(str, n) {
  return str.repeat(n);
}

console.log(repeatStr('Hello', 3)); // HelloHelloHello
