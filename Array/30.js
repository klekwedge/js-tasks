// Массив длин строковых элементов
// Используя метод map() напишите код, который
// получает из массива строк новый массив, содержащий их длины.

const vegetables = ['Капуста', 'Репа', 'Редиска', 'Морковка'];

console.log(vegetables.map((item) => item.length)); // [ 7, 4, 7, 8 ]
